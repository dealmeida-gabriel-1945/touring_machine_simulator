# Flags to represent the presence of each of parameters
run_resume = True
run_verbose = False
run_step = False
steps = 0

first_word = ""

# Possible parameter that can be at list of args
possible_params = [
    ['-resume', '-r'],
    ['-verbose', '-v'],
    ['-step', '-s'],
]
